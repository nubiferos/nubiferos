---
# Check if encryption is enabled and warn user if not
# Runs after partition module creates partitions but before mount
# This allows us to detect LUKS and warn before data is copied

dontChroot: true
timeout: 300

script:
    - command: "/usr/local/bin/calamares-check-encryption.sh"
      timeout: 300
