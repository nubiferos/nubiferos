---
# Post-install cleanup - remove installer user and live-boot artifacts
# Runs in chroot near the end of installation

dontChroot: false
timeout: 60

script:
    - command: "/usr/local/bin/calamares-post-install-cleanup.sh"
      timeout: 60
