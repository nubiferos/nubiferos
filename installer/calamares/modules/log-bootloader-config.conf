---
# Log bootloader configuration for debugging

dontChroot: false
timeout: 30

script:
    - command: "if [ -x /usr/local/bin/calamares-config-logger.sh ]; then /usr/local/bin/calamares-config-logger.sh; else echo 'WARNING: calamares-config-logger.sh not found'; fi"
      timeout: 30
