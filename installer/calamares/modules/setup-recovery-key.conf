---
# Generate LUKS recovery key after partition setup
# Runs in live environment (not chroot) right after partition creates LUKS

dontChroot: true
timeout: 30

script:
    - command: "/usr/local/bin/calamares-setup-recovery-key.sh"
      timeout: 30
