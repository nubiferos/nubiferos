---
# NetInstall module - simpler alternative to packagechooser
# Uses Qt widgets instead of QML (no threading issues)

groupsUrl: file:///etc/calamares/modules/netinstall-packages.yaml

required: true

label:
    sidebar: "Packages"
    title: "Package Selection"
    step: "Select packages to install (Security tools are required)"
