---
# Show recovery key to user during installation
# Runs in live environment (not chroot) with access to display

dontChroot: true
timeout: 300

script:
    - command: "/usr/local/bin/calamares-show-recovery-key.sh"
      timeout: 300
