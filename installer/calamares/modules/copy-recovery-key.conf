---
# Copy recovery key to user's desktop
# Runs in chroot after users module creates the user

dontChroot: false
timeout: 30

script:
    - command: "/usr/local/bin/calamares-copy-recovery-key.sh"
      timeout: 30
