---
# Prepare system for bootloader installation
# Ensures device nodes are visible and ready

dontChroot: false
timeout: 60

script:
    - command: "/usr/local/bin/calamares-prepare-bootloader.sh"
      timeout: 60
