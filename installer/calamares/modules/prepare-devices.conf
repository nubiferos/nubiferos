---
# Prepare devices before installation
# Ensures all block devices are visible and ready

dontChroot: true
timeout: 60

script:
    - command: "/usr/local/bin/calamares-prepare-devices.sh"
      timeout: 60
