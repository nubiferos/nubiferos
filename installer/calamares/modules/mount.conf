---
# Mount module configuration
# Mounts the target partitions before installation

extraMounts:
    - device: proc
      fs: proc
      mountPoint: /proc
    - device: sys
      fs: sysfs
      mountPoint: /sys
    - device: tmpfs
      fs: tmpfs
      mountPoint: /run
